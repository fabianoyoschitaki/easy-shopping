<resources>

    <!-- Material Design -->
    <style name="IBoughtMaterialTheme" parent="IBoughtMaterialTheme.Base">

    </style>

    <style name="FacebookLoginButton">
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_gravity">center_horizontal</item>
    </style>

    <!-- Material Design -->
    <style name="IBoughtMaterialTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <!-- Cor primaria do aplicativo -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- Variacao escura da cor primaria para a status bar e contextual app bars -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!-- Cor de acentuacao para as views (checkbox, textfield etc) -->
        <item name="colorAccent">@color/colorAccent</item>

        <!-- http://stackoverflow.com/questions/6921640/how-to-set-text-color-in-android-app-for-all-text -->
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:textColorSecondary">@color/textColorSecondary</item>
        <item name="android:textColorTertiary">@color/textColorTertiary</item>
    </style>

    <style name="IBoughtMaterialTheme.AppBarOverlay"
        parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="IBoughtMaterialTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
